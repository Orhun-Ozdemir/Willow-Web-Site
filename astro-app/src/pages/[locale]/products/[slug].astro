---
import PageLayout from "@/layouts/PageLayout.astro";
import ProductGallery from "@/components/ProductGallery.astro";
import AIAnswerBlock from "@/components/AIAnswerBlock.astro";
import { loadContent } from "@/lib/content";
import { localizedValue, localizeItem, locales, type Locale } from "@/lib/cms";
import { buildAlternates, DEFAULT_OG_IMAGE, ORGANIZATION_SCHEMA, WEBSITE_SCHEMA, SITE_ORIGIN } from "@/lib/seo";
import { aiMetaForPage, appendSchemaNodes, faqSchemaNode, stripHtml, type AiFaqItem } from "@/lib/ai-search";

export const prerender = false;

const { locale, slug } = Astro.params as { locale: Locale; slug: string };
const content = await loadContent();
const rawProduct = (content.products || []).find((p: any) => p.slug === slug || p.id === slug);

if (!rawProduct) {
  return Astro.redirect(`/${locale}/products`);
}

const product = localizeItem(rawProduct, locale);
const seo = rawProduct.seo?.[locale] || {};
const titleLower = (product.title || "").toLowerCase();
const pickText = (value: any) => localizedValue(value, locale);

// Category text mappings
const categoryMap: Record<string, string> = {
  modules: "Use this module as the embedded core of low-power connected devices where firmware, RF performance and long-term maintainability matter.",
  environment: "Use this sensor family for environmental telemetry, alerts and remote monitoring in industrial, utility and field conditions.",
  tracking: "Use this device family to detect movement, access, safety events and asset state changes across distributed sites.",
  industrial: "Use this product to connect industrial equipment, Modbus devices and telemetry workflows into a unified platform."
};
const localizedFitText = {
  en: categoryMap[product.category] || "Use this product as part of a reliable connected product ecosystem.",
  tr: "Bu ürünü güvenilir bir bağlı ürün ekosisteminin parçası olarak konumlandırın.",
  de: "Nutzen Sie dieses Produkt als Teil eines zuverlässigen vernetzten Produktsystems.",
  fr: "Positionnez ce produit comme une brique d'un écosystème connecté fiable.",
  es: "Use este producto como parte de un ecosistema conectado fiable.",
  it: "Usa questo prodotto come parte di un ecosistema connesso affidabile.",
  ar: "استخدم هذا المنتج كجزء من منظومة منتجات متصلة وموثوقة.",
  ja: "この製品を信頼性の高い接続システムの一部として活用できます。",
};
const fitText = pickText(localizedFitText);

const snapshotMap: Record<string, string> = {
  modules: "Embedded core for low-power device builds.",
  environment: "Field telemetry for environmental monitoring.",
  tracking: "Event detection for distributed operations.",
  industrial: "Industrial bridge for machine-side data."
};
const localizedSnapshotText = {
  en: snapshotMap[product.category] || "Built for field telemetry.",
  tr: "Saha telemetrisi için tasarlanmış teknik yapı.",
  de: "Technische Basis für Feldtelemetrie.",
  fr: "Base technique pour la télémétrie terrain.",
  es: "Base técnica para telemetría de campo.",
  it: "Base tecnica per la telemetria sul campo.",
  ar: "بنية تقنية للقياس عن بعد في الميدان.",
  ja: "現場テレメトリ向けの技術構成。",
};
const snapshotText = pickText(localizedSnapshotText);

const byCategory: Record<string, any> = {
  modules: {
    useTitle: "Custom connected devices",
    use: "Use this module when you need a low-power LoRaWAN core for a custom sensor, controller or field device.",
    roleTitle: "Embedded core",
    role: "It sits at the device layer, where MCU logic, RF behavior, GPIO mapping and firmware maintainability decide product reliability.",
    delivered: ["Firmware architecture and low-power logic", "LoRaWAN payload design and RF validation", "Prototype-to-production embedded support"]
  },
  environment: {
    useTitle: "Environmental monitoring",
    use: "Use this sensor family for indoor, outdoor or field conditions where measurements must become alerts, dashboards and reports.",
    roleTitle: "Measurement node",
    role: "It captures field conditions and sends structured telemetry into the platform for monitoring, reporting and operator action.",
    delivered: ["Sensor configuration and reporting intervals", "Backend ingestion and PostgreSQL time-series model", "Dashboards, thresholds and mobile notifications"]
  },
  tracking: {
    useTitle: "Event and asset state tracking",
    use: "Use this device when movement, access, safety, tilt or route events need to be visible without manual site checks.",
    roleTitle: "Event trigger",
    role: "It becomes the field signal that turns physical state changes into operational alerts and history.",
    delivered: ["Event logic and payload design", "Alert routing and operator workflow", "Map, log or admin panel integration"]
  },
  industrial: {
    useTitle: "Industrial telemetry integration",
    use: "Use this product when existing meters, PLCs or field assets need to report operational data into a modern digital platform.",
    roleTitle: "Industrial bridge",
    role: "It connects machine-side data with APIs, PostgreSQL, web dashboards and mobile operational tools.",
    delivered: ["Register mapping and device configuration", "API ingestion and PostgreSQL schema", "Admin dashboard, reporting and integrations"]
  }
};

const decision = { ...(byCategory[product.category] || {
  useTitle: "Field monitoring",
  use: "Use this product where physical-world data must be measured, transmitted and turned into operational decisions.",
  roleTitle: "Telemetry source",
  role: "It feeds the digital layer with structured measurements, events and device state.",
  delivered: ["Firmware and connectivity configuration", "Backend ingestion and PostgreSQL model", "Dashboard, mobile and alerting workflow"]
}) };

if (titleLower.includes("panic")) {
  decision.useTitle = "Safety and emergency workflows";
  decision.use = "Use this device where operators need a physical emergency trigger connected to remote alerts and response workflows.";
}
if (titleLower.includes("modbus")) {
  decision.useTitle = "Legacy equipment integration";
  decision.roleTitle = "Modbus-to-platform bridge";
}
if (titleLower.includes("ultrasonic") || titleLower.includes("level")) {
  decision.useTitle = "Tank, manhole and level monitoring";
  decision.use = "Use this product where level, distance or fill state must be monitored remotely across distributed field assets.";
}
if (titleLower.includes("soil")) {
  decision.useTitle = "Agriculture and field research";
}

if (locale !== "en") {
  const localizedDecision = {
    useTitle: { tr: "Saha izleme ve bağlı ürün kullanımı", de: "Feldüberwachung und vernetzte Produktnutzung", fr: "Surveillance terrain et usage connecté", es: "Monitorización de campo y producto conectado", it: "Monitoraggio sul campo e prodotto connesso", ar: "مراقبة ميدانية واستخدام منتج متصل", ja: "現場監視と接続製品の活用" },
    use: { tr: "Bu ürünü fiziksel dünyadan gelen verilerin güvenilir şekilde ölçülmesi, iletilmesi ve operasyonel kararlara dönüştürülmesi gereken yapılarda kullanın.", de: "Setzen Sie dieses Produkt ein, wenn physische Felddaten zuverlässig gemessen, übertragen und in operative Entscheidungen überführt werden müssen.", fr: "Utilisez ce produit lorsque les données physiques doivent être mesurées, transmises et transformées en décisions opérationnelles.", es: "Use este producto cuando los datos físicos deban medirse, transmitirse y convertirse en decisiones operativas.", it: "Usa questo prodotto quando i dati fisici devono essere misurati, trasmessi e trasformati in decisioni operative.", ar: "استخدم هذا المنتج عندما تحتاج بيانات العالم الحقيقي إلى القياس والإرسال والتحويل إلى قرارات تشغيلية.", ja: "現場データを計測し、送信し、運用判断につなげる必要がある構成で利用できます。" },
    roleTitle: { tr: "Telemetri kaynağı", de: "Telemetriequelle", fr: "Source de télémétrie", es: "Fuente de telemetría", it: "Sorgente di telemetria", ar: "مصدر القياس عن بعد", ja: "テレメトリソース" },
    role: { tr: "Cihaz katmanından gelen ölçüm ve olay verilerini dijital sisteme taşıyan saha sinyalidir.", de: "Es liefert Mess- und Ereignisdaten aus der Geräteebene in das digitale System.", fr: "Il transmet les mesures et événements du terrain vers le système numérique.", es: "Lleva mediciones y eventos desde la capa de dispositivo hacia el sistema digital.", it: "Porta misure ed eventi dal livello dispositivo al sistema digitale.", ar: "ينقل القياسات والأحداث من طبقة الجهاز إلى النظام الرقمي.", ja: "デバイス層の計測値やイベントをデジタルシステムへ送る現場信号です。" },
    delivered: {
      tr: ["Cihaz ve bağlantı yapılandırması", "Backend veri alımı ve PostgreSQL modeli", "Dashboard, mobil ve alarm iş akışı"],
      de: ["Geräte- und Konnektivitätskonfiguration", "Backend-Ingestion und PostgreSQL-Modell", "Dashboard-, Mobile- und Alarm-Workflow"],
      fr: ["Configuration dispositif et connectivité", "Ingestion backend et modèle PostgreSQL", "Dashboard, mobile et workflow d'alertes"],
      es: ["Configuración de dispositivo y conectividad", "Ingesta backend y modelo PostgreSQL", "Dashboard, móvil y flujo de alertas"],
      it: ["Configurazione dispositivo e connettività", "Ingestion backend e modello PostgreSQL", "Dashboard, mobile e flusso di avvisi"],
      ar: ["إعداد الجهاز والاتصال", "استقبال البيانات في الخلفية ونموذج PostgreSQL", "لوحة تحكم وتطبيق جوال وسير عمل للتنبيهات"],
      ja: ["デバイスと通信設定", "バックエンド取り込みとPostgreSQLモデル", "ダッシュボード、モバイル、アラート運用"],
    },
  };
  decision.useTitle = pickText(localizedDecision.useTitle);
  decision.use = pickText(localizedDecision.use);
  decision.roleTitle = pickText(localizedDecision.roleTitle);
  decision.role = pickText(localizedDecision.role);
  decision.delivered = localizedDecision.delivered[locale] || localizedDecision.delivered.tr;
}

const galleryImages = Array.isArray(product.images) ? product.images.filter(Boolean) : (product.image ? [product.image] : []);

// Technical sections
const techBlockLabels = {
  technicalSummary: { en: "Technical Summary", tr: "Teknik Özet", de: "Technische Zusammenfassung", fr: "Résumé technique", es: "Resumen técnico", it: "Sintesi tecnica", ar: "ملخص تقني", ja: "技術概要" },
  useCases: { en: "Use Cases", tr: "Kullanım Senaryoları", de: "Anwendungsfälle", fr: "Cas d'usage", es: "Casos de uso", it: "Casi d'uso", ar: "حالات الاستخدام", ja: "ユースケース" },
  specifications: { en: "Specifications", tr: "Teknik Özellikler", de: "Spezifikationen", fr: "Spécifications", es: "Especificaciones", it: "Specifiche", ar: "المواصفات", ja: "仕様" },
};
const techBlocks = [
  { label: pickText(techBlockLabels.technicalSummary), html: typeof product.technicalSummary === "string" ? product.technicalSummary : "" },
  { label: pickText(techBlockLabels.useCases), html: typeof product.useCases === "string" ? product.useCases : "" },
  { label: pickText(techBlockLabels.specifications), html: typeof product.specifications === "string" ? product.specifications : "" }
].filter((block) => block.html && String(block.html).trim());

const dynamicLabels = {
  productModel: { en: "Product Model", tr: "Ürün Modeli", de: "Produktmodell", fr: "Modèle produit", es: "Modelo de producto", it: "Modello prodotto", ar: "نموذج المنتج", ja: "製品モデル" },
  productType: { en: "Product Type", tr: "Ürün Tipi", de: "Produkttyp", fr: "Type de produit", es: "Tipo de producto", it: "Tipo di prodotto", ar: "نوع المنتج", ja: "製品タイプ" },
  batteryLife: { en: "Battery Life", tr: "Batarya Ömrü", de: "Batterielaufzeit", fr: "Autonomie", es: "Duración de batería", it: "Durata batteria", ar: "عمر البطارية", ja: "バッテリー寿命" },
  communicationRange: { en: "Communication Range", tr: "İletişim Menzili", de: "Kommunikationsreichweite", fr: "Portée de communication", es: "Alcance de comunicación", it: "Portata comunicazione", ar: "مدى الاتصال", ja: "通信範囲" },
  applications: { en: "Applications", tr: "Uygulama Alanları", de: "Anwendungen", fr: "Applications", es: "Aplicaciones", it: "Applicazioni", ar: "التطبيقات", ja: "用途" },
  whereUsed: { en: "Where this product is used.", tr: "Bu ürünün kullanıldığı alanlar.", de: "Wo dieses Produkt eingesetzt wird.", fr: "Où ce produit est utilisé.", es: "Dónde se utiliza este producto.", it: "Dove viene usato questo prodotto.", ar: "أين يُستخدم هذا المنتج.", ja: "この製品が使われる場所" },
  technicalGroup: { en: "Technical Group", tr: "Teknik Grup", de: "Technische Gruppe", fr: "Groupe technique", es: "Grupo técnico", it: "Gruppo tecnico", ar: "مجموعة تقنية", ja: "技術グループ" },
  specifications: { en: "Specifications", tr: "Teknik Özellikler", de: "Spezifikationen", fr: "Spécifications", es: "Especificaciones", it: "Specifiche", ar: "المواصفات", ja: "仕様" },
};
const dynamicLabel = (key: keyof typeof dynamicLabels) => pickText(dynamicLabels[key]);
const labelizeKey = (key: string) => String(key || "")
  .replace(/[_-]+/g, " ")
  .replace(/\b\w/g, (char) => char.toUpperCase());
const stringifySpecValue = (value: any) => {
  if (value == null) return "";
  if (typeof value === "object") return JSON.stringify(value);
  return String(value);
};
const normalizeSpecGroups = (item: any) => {
  const groups = Array.isArray(item.specGroups) ? item.specGroups : [];
  const normalized = groups.map((group: any) => ({
    title: group.title || "Technical Details",
    rows: Array.isArray(group.rows) ? group.rows.filter((row: any) => row?.label || row?.value) : [],
    lists: Array.isArray(group.lists) ? group.lists.map((list: any) => ({
      title: list.title || "Highlights",
      items: Array.isArray(list.items) ? list.items.filter(Boolean) : []
    })).filter((list: any) => list.items.length) : []
  })).filter((group: any) => group.rows.length || group.lists.length);
  if (normalized.length) return normalized;

  if (!item.specifications || typeof item.specifications !== "object" || Array.isArray(item.specifications)) {
    return [];
  }

  const rootRows: any[] = [];
  const rootLists: any[] = [];
  const objectGroups: any[] = [];

  Object.entries(item.specifications).forEach(([key, value]: [string, any]) => {
    if (value == null || value === "") return;
    if (Array.isArray(value)) {
      const items = value.map(stringifySpecValue).filter(Boolean);
      if (items.length) rootLists.push({ title: labelizeKey(key), items });
      return;
    }
    if (typeof value === "object") {
      const rows: any[] = [];
      const lists: any[] = [];
      Object.entries(value).forEach(([nestedKey, nestedValue]: [string, any]) => {
        if (nestedValue == null || nestedValue === "") return;
        if (Array.isArray(nestedValue)) {
          const items = nestedValue.map(stringifySpecValue).filter(Boolean);
          if (items.length) lists.push({ title: labelizeKey(nestedKey), items });
        } else {
          rows.push({ label: labelizeKey(nestedKey), value: stringifySpecValue(nestedValue) });
        }
      });
      if (rows.length || lists.length) objectGroups.push({ title: labelizeKey(key), rows, lists });
      return;
    }
    rootRows.push({ label: labelizeKey(key), value: String(value) });
  });

  return [
    ...(rootRows.length || rootLists.length ? [{ title: dynamicLabel("specifications") || "Specifications", rows: rootRows, lists: rootLists }] : []),
    ...objectGroups
  ];
};
const dynamicFacts = [
  { label: dynamicLabel("productType"), value: product.type },
  { label: dynamicLabel("batteryLife"), value: product.batteryLife || product.battery_life },
  { label: dynamicLabel("communicationRange"), value: product.communicationRange || product.communication_range }
].filter((fact) => fact.value);
const productApplications = Array.isArray(product.applications) ? product.applications.filter(Boolean) : [];
const dynamicSpecGroups = normalizeSpecGroups(product); console.log("dynamicSpecGroups:", JSON.stringify(dynamicSpecGroups));

const productAiIntro = pickText({
  en: `${product.title} is a WillowSoft ${product.category || "IoT"} product for connected field telemetry.`,
  tr: `${product.title}, bağlı saha telemetrisi için geliştirilen bir WillowSoft ${product.category || "IoT"} ürünüdür.`,
  de: `${product.title} ist ein WillowSoft-${product.category || "IoT"}-Produkt für vernetzte Feldtelemetrie.`,
  fr: `${product.title} est un produit WillowSoft ${product.category || "IoT"} pour la télémétrie terrain connectée.`,
  es: `${product.title} es un producto WillowSoft ${product.category || "IoT"} para telemetría de campo conectada.`,
  it: `${product.title} è un prodotto WillowSoft ${product.category || "IoT"} per la telemetria connessa sul campo.`,
  ar: `${product.title} هو منتج ${product.category || "IoT"} من WillowSoft للقياس الميداني المتصل.`,
  ja: `${product.title} は、接続された現場テレメトリ向けの WillowSoft ${product.category || "IoT"} 製品です。`,
});
const productAiAnswer = stripHtml(`${productAiIntro} ${product.shortDescription || ""} ${decision.use || ""}`);
const productFaqLabels = {
  useQuestion: {
    en: `What is ${product.title} used for?`,
    tr: `${product.title} ne için kullanılır?`,
    de: `Wofür wird ${product.title} verwendet?`,
    fr: `À quoi sert ${product.title} ?`,
    es: `¿Para qué se utiliza ${product.title}?`,
    it: `A cosa serve ${product.title}?`,
    ar: `ما استخدام ${product.title}؟`,
    ja: `${product.title} は何に使いますか？`,
  },
  integrationQuestion: {
    en: `Can ${product.title} be integrated with software platforms?`,
    tr: `${product.title} yazılım platformlarıyla entegre edilebilir mi?`,
    de: `Kann ${product.title} in Softwareplattformen integriert werden?`,
    fr: `${product.title} peut-il être intégré à des plateformes logicielles ?`,
    es: `¿Puede ${product.title} integrarse con plataformas de software?`,
    it: `${product.title} può essere integrato con piattaforme software?`,
    ar: `هل يمكن دمج ${product.title} مع منصات برمجية؟`,
    ja: `${product.title} はソフトウェア基盤と連携できますか？`,
  },
  selectionQuestion: {
    en: `When should a buyer choose ${product.title}?`,
    tr: `Bir alıcı ${product.title} ürününü ne zaman seçmeli?`,
    de: `Wann sollte ein Käufer ${product.title} wählen?`,
    fr: `Quand choisir ${product.title} ?`,
    es: `¿Cuándo debería elegirse ${product.title}?`,
    it: `Quando scegliere ${product.title}?`,
    ar: `متى يجب اختيار ${product.title}؟`,
    ja: `${product.title} はどのような場合に選ぶべきですか？`,
  },
  integrationAnswer: {
    en: "It can be used as part of a complete WillowSoft hardware, firmware, backend and dashboard stack.",
    tr: "Eksiksiz bir WillowSoft donanım, firmware, backend ve dashboard yığınının parçası olarak kullanılabilir.",
    de: "Es kann als Teil eines vollständigen WillowSoft-Stacks aus Hardware, Firmware, Backend und Dashboard genutzt werden.",
    fr: "Il peut être utilisé dans une pile WillowSoft complète avec matériel, firmware, backend et tableau de bord.",
    es: "Puede usarse como parte de un stack completo de WillowSoft con hardware, firmware, backend y dashboard.",
    it: "Può essere usato come parte di uno stack WillowSoft completo con hardware, firmware, backend e dashboard.",
    ar: "يمكن استخدامه ضمن حزمة WillowSoft متكاملة تشمل العتاد والبرمجيات الثابتة والخلفية ولوحة التحكم.",
    ja: "WillowSoft のハードウェア、ファームウェア、バックエンド、ダッシュボードを含む統合スタックの一部として利用できます。",
  },
};
const productFaqItems: AiFaqItem[] = [
  { question: pickText(productFaqLabels.useQuestion), answer: stripHtml(decision.use) },
  { question: pickText(productFaqLabels.integrationQuestion), answer: stripHtml(pickText(productFaqLabels.integrationAnswer)) },
  { question: pickText(productFaqLabels.selectionQuestion), answer: stripHtml(fitText) },
].filter((item) => item.question && item.answer);
const productAiMeta = aiMetaForPage(content, "products", locale);

// Schema.org Graph
const graphSchema = appendSchemaNodes({
  "@context": "https://schema.org",
  "@graph": [
    ORGANIZATION_SCHEMA,
    WEBSITE_SCHEMA,
    {
      "@type": "BreadcrumbList",
      "itemListElement": [
        { "@type": "ListItem", "position": 1, "name": "Home", "item": `${SITE_ORIGIN}/${locale}` },
        { "@type": "ListItem", "position": 2, "name": "Products", "item": `${SITE_ORIGIN}/${locale}/products` },
        { "@type": "ListItem", "position": 3, "name": product.title }
      ]
    },
    {
      "@type": "Product",
      "name": product.title,
      "sku": product.slug || product.id,
      "image": product.image ? [`${SITE_ORIGIN}/${product.image}`.replace(/([^:])\/\//g, "$1/")] : [],
      "description": product.shortDescription || "",
      "brand": { "@type": "Brand", "name": "WillowSoft" },
      "manufacturer": { "@id": `${SITE_ORIGIN}/#org` },
      "category": product.category || "Product",
      "offers": {
        "@type": "Offer",
        "priceCurrency": "USD",
        "availability": "https://schema.org/InStock",
        "seller": { "@id": `${SITE_ORIGIN}/#org` },
        "url": `${SITE_ORIGIN}/${locale}/products/${slug}`
      }
    }
  ]
}, [faqSchemaNode(`${SITE_ORIGIN}/${locale}/products/${slug}`, productFaqItems)]);

// Localized UI Labels
const t = {
  technicalSnapshot: { en: "Technical Snapshot", tr: "Teknik Özet", de: "Technische Momentaufnahme", fr: "Aperçu technique", es: "Resumen técnico", it: "Panoramica tecnica", ar: "لقطة فنية", ja: "技術概要" },
  capability: { en: "Capability", tr: "Yetenek", de: "Fähigkeit", fr: "Capacité", es: "Capacidad", it: "Funzionalità", ar: "القدرة", ja: "機能" },
  readyText: { en: "Industrial deployment ready.", tr: "Endüstriyel kuruluma hazır.", de: "Bereit für den industriellen Einsatz.", fr: "Prêt pour le déploiement industriel.", es: "Listo para implementación industrial.", it: "Pronto per l'installazione industriale", ar: "جاهز للنشر الصناعي.", ja: "産業用途への即時導入が可能" },
  readyDesc: {
    en: "Use this product as part of a complete WillowSoft hardware, firmware, backend and dashboard stack.",
    tr: "Bu ürünü eksiksiz bir WillowSoft donanım, gömülü yazılım, arka plan ve kontrol paneli yığınının parçası olarak kullanın.",
    de: "Nutzen Sie dieses Produkt als Teil eines vollständigen WillowSoft-Hardware-, Firmware-, Backend- und Dashboard-Stacks.",
    fr: "Utilisez ce produit dans le cadre d'une pile complète de matériel, de micrologiciel, de backend et de tableau de bord WillowSoft.",
    es: "Utilice este producto como parte de una pila completa de hardware, firmware, backend y panel de control de WillowSoft.",
    it: "Utilizza questo prodotto come parte di uno stack completo WillowSoft di hardware, firmware, backend e dashboard.",
    ar: "استخدم هذا CNBC كجزء من أجهزة وبرامج مدمجة ولوحة تحكم متكاملة من WillowSoft.",
    ja: "無線デバイス、デバイスファームウェア、クラウドバックエンド、およびダッシュボードを含む統合システムとして導入できます。"
  },
  gallery: { en: "Gallery", tr: "Galeri", de: "Galerie", fr: "Galerie", es: "Galería", it: "Galleria", ar: "معرض الصور", ja: "製品ギャラリー" },
  whereFits: { en: "Where It Fits", tr: "Uyumlu Olduğu Yapı", de: "Wo es hineinpasst", fr: "Où cela s'intègre", es: "Dónde encaja", it: "Dove si integra", ar: "مكان الاستخدام", ja: "システム構成" },
  fitsTitle: { en: "From device data to operational decisions.", tr: "Cihaz verilerinden operasyonel kararlara.", de: "Von Gerätedaten zu betrieblichen Entscheidungen.", fr: "Des données de l'appareil aux décisions opérationnelles.", es: "Desde los datos del dispositivo hasta las decisiones operativas.", it: "Dai dati del dispositivo alle decisioni operative.", ar: "من بيانات الجهاز إلى القرارات التشغيلية.", ja: "デバイスデータから業務の意思決定まで" },
  fitsLi1: {
    en: "Can be evaluated as a standalone product or part of a complete connected system.",
    tr: "Tek başına bir ürün olarak veya eksiksiz bir bağlantılı sistemin parçası olarak değerlendirilebilir.",
    de: "Kann als eigenständiges Produkt oder als Teil eines vollständig vernetzten Systems bewertet werden.",
    fr: "Peut être évalué comme un produit autonome ou dans le cadre d'un système connecté complet.",
    es: "Puede evaluarse como un producto independiente o como parte de un sistema conectado completo.",
    it: "Può essere valutato come prodotto autonomo o come parte di un sistema connesso completo.",
    ar: "يمكن تقييمه كمنتج مستقل أو كجزء من نظام متصل متكامل.",
    ja: "スタンドアロンのハードウェアとして、または統合されたエンドツーエンドのシステムとして評価可能です。"
  },
  fitsLi2: {
    en: "Useful when field data must become dashboards, alerts, reports or integrations.",
    tr: "Saha verilerinin kontrol panellerine, uyarılara, raporlara veya entegrasyonlara dönüşmesi gerektiğinde kullanışlıdır.",
    de: "Nützlich, wenn Felddaten in Dashboards, Warnungen, Berichte oder Integrationen einfließen müssen.",
    fr: "Utile lorsque les données de terrain doivent se transformer en tableaux de bord, alertes, rapports ou intégrations.",
    es: "Útil cuando los datos de campo deben convertirse en paneles de control, alertas, informes o integraciones.",
    it: "Utile quando i dati sul campo devono tradursi in dashboard, avvisi, report o integrazioni.",
    ar: "مفيد عندما يجب أن تتحول البيانات الميدانية إلى لوحات تحكم أو تنبيهات أو تقارير أو عمليات ربط.",
    ja: "現場での測定データをダッシュボード、アラート、定期レポート、または外部システム連携に活用する際に最適です。"
  },
  fitsLi3: {
    en: "Related firmware, backend, PostgreSQL, web/admin and mobile layers can be delivered by WillowSoft.",
    tr: "İlgili gömülü yazılım, arka plan, PostgreSQL, web/yönetim ve mobil katmanlar WillowSoft tarafından teslim edilebilir.",
    de: "Zugehörige Firmware, Backends, PostgreSQL, Web/Admin- und mobile Schichten können von WillowSoft geliefert werden.",
    fr: "Les couches de micrologiciel, de backend, de PostgreSQL, de site web/d'administration et de mobile associées peuvent être fournies par WillowSoft.",
    es: "Las capas de firmware, backend, PostgreSQL, web/administración y móvil relacionadas pueden ser entregadas por WillowSoft.",
    it: "I relativi livelli di firmware, backend, PostgreSQL, portale web/amministratore e app mobile possono essere forniti da WillowSoft.",
    ar: "يمكن لـ WillowSoft تقديم البرامج المدمجة ذات الصلة، والخلفية، وقاعدة بيانات PostgreSQL، وطبقات الويب/الإدارة والجوال.",
    ja: "対応するファームウェア、APIサーバー、PostgreSQLデータベース、Web管理画面、モバイルアプリの全レイヤーをWillowSoftが開発提供します。"
  },
  buyerChecklist: { en: "Buyer Checklist", tr: "Alıcı Kontrol Listesi", de: "Käufer-Checkliste", fr: "Liste de contrôle de l'acheteur", es: "Lista de verificación del comprador", it: "Checklist per l'acquisto", ar: "قائمة التحقق للمشتري", ja: "導入計画チェックリスト" },
  buyerTitle: { en: "What this product helps you decide before deployment.", tr: "Kurulumdan önce bu ürünün karar vermenize yardımcı olduğu konular.", de: "Was dieses Produkt Ihnen vor der Bereitstellung zu entscheiden hilft.", fr: "Ce que ce produit vous aide à décider avant le déploiement.", es: "Lo que este producto le ayuda a decidir antes de la implementación.", it: "Cosa ti aiuta a decidere questo prodotto prima dell'installazione.", ar: "ما يساعدك هذا المنتج على قراره قبل عملية النشر.", ja: "導入前に検討すべき要素" },
  planDeployment: { en: "Plan Deployment", tr: "Kurulum Planla", de: "Bereitstellung planen", fr: "Planifier le déploiement", es: "Planificar la implementación", it: "Pianifica l'installazione", ar: "تخطيط النشر", ja: "導入計画を開始" },
  useCase: { en: "Use Case", tr: "Kullanım Durumu", de: "Anwendungsfall", fr: "Cas d'usage", es: "Caso de uso", it: "Caso d'uso", ar: "حالة الاستخدام", ja: "ユースケース" },
  systemRole: { en: "System Role", tr: "Sistem Rolü", de: "Systemrolü", fr: "Rôle du système", es: "Rol del sistema", it: "Ruolo del sistema", ar: "دور النظام", ja: "システム上の役割" },
  deliveredWith: { en: "Delivered With", tr: "Birlikte Teslim Edilenler", de: "Geliefert mit", fr: "Livré avec", es: "Entregado con", it: "Fornito con", ar: "يُسلم مع", ja: "提供内容" },
  hwSwPath: { en: "Hardware plus software path", tr: "Donanım artı yazılım yolu", de: "Hardware- und Softwarepfad", fr: "Parcours matériel et logiciel", es: "Ruta de hardware más software", it: "Percorso hardware e software", ar: "مسار الأجهزة والبرامج", ja: "ハードウェア ＋ ソフトウェア開発" },
  sysArch: { en: "System Architecture", tr: "Sistem Mimarisi", de: "Systemarchitektur", fr: "Architecture système", es: "Arquitectura del sistema", it: "Architettura del sistema", ar: "بنية النظام", ja: "システム構成図" },
  sysTitle: { en: "A product page should show the whole operating path.", tr: "Bir ürün sayfası tüm çalışma yolunu göstermelidir.", de: "Eine Produktseite sollte den gesamten Betriebspfad zeigen.", fr: "Une page produit doit montrer tout le parcours opérationnel.", es: "Una página de producto debe mostrar toda la ruta operativa.", it: "Una pagina prodotto dovrebbe mostrare l'intero percorso operativo.", ar: "يجب أن توضح صفحة المنتج مسار التشغيل بالكامل.", ja: "デバイスからクラウドまでの接続図" },
  sysDesc: {
    en: "WillowSoft products are strongest when they are presented as part of a complete operational system: sensor, connectivity, backend, database, dashboard, mobile and alerting.",
    tr: "WillowSoft ürünleri, eksiksiz bir operasyonel sistemin parçası olarak sunulduğunda en güçlü yönlerini ortaya koyar: sensör, bağlantı, arka uç, veritabanı, kontrol paneli, mobil ve uyarı.",
    de: "WillowSoft-Produkte sind am stärksten, wenn sie als Teil eines vollständigen Betriebssystems präsentiert werden: Sensor, Konnektivität, Backend, Datenbank, Dashboard, Mobile und Alarmierung.",
    fr: "Les produits WillowSoft sont les plus performants lorsqu'ils sont présentés comme faisant partie d'un système opérationnel complet : capteur, connectivité, backend, base de données, tableau de bord, mobile et alerte.",
    es: "Los productos de WillowSoft son más fuertes cuando se presentan como parte de un sistema operativo completo: sensor, conectividad, backend, base de datos, panel de control, móvil y alertas.",
    it: "I prodotti WillowSoft offrono il massimo valore quando presentati come parte di un sistema operativo completo: sensore, connettività, backend, database, dashboard, app mobile e avvisi.",
    ar: "تكون منتجات WillowSoft في أقوى حالاتها عندما تُقدم كجزء من نظام تشغيلي متكامل: المستشعر، والاتصال، والخلفية، وقاعدة البيانات، ولوحة التحكم، والجوال، والتنبيهات.",
    ja: "WillowSoftのデバイスは、センサー、ネットワーク、サーバー、データベース、ダッシュボード、モバイル通知までを網羅した統合システムの一部として導入することで、最大の効果を発揮します。"
  },
  needInProject: { en: "Need this in a project?", tr: "Projelerinizde buna mı ihtiyacınız var?", de: "Benötigen Sie dies in einem Projekt?", fr: "Besoin de cela dans un projet ?", es: "¿Necesita esto en un proyecto?", it: "Ti serve in un progetto?", ar: "هل تحتاج هذا في مشروعك؟", ja: "プロジェクトへの導入をご検討中ですか？" },
  askProductFit: { en: "Ask for product fit, integration path and deployment scope.", tr: "Ürün uyumu, entegrasyon yolu ve kurulum kapsamı hakkında bilgi isteyin.", de: "Fragen Sie nach Produktpassung, Integrationspfad und Bereitstellungsumfang.", fr: "Demandez l'adéquation du produit, le parcours d'intégration et la portée du déploiement.", es: "Pregunte por la idoneidad del producto, la ruta de integración y el alcance de la implementación.", it: "Chiedi informazioni sulla compatibilità del prodotto, il percorso di integrazione e l'ambito dell'installazione.", ar: "اسأل عن مدى ملاءمة المنتج ومسار الربط ونطاق النشر.", ja: "製品の適合性、既存システムとの連携方法、および導入規模についてお問い合わせください。"
  },
  startProject: { en: "Start Project", tr: "Projeyi Başlat", de: "Projekt starten", fr: "Démarrer le projet", es: "Iniciar proyecto", it: "Avvia progetto", ar: "بدء المشروع", ja: "プロジェクト相談" },
  contactSales: { en: "Contact Sales", tr: "Satışla İletişime Geçin", de: "Vertrieb kontaktieren", fr: "Contacter le service commercial", es: "Contactar a ventas", it: "Contatta le vendite", ar: "الاتصال بالمبيعات", ja: "営業チームに相談" },
  backToProducts: { en: "Back to Products", tr: "Ürünlere Dön", de: "Zurück zu Produkten", fr: "Retour aux Produkten", es: "Volver a productos", it: "Torna ai prodotti", ar: "العودة إلى المنتجات", ja: "製品一覧へ戻る" },
  reqInfo: { en: "Request Information", tr: "Bilgi Talep Et", de: "Informationen anfordern", fr: "Demander des informations", es: "Solicitar información", it: "Richiedi informazioni", ar: "طلب معلومات", ja: "資料請求・お問い合わせ" },
  flowDevice: { en: "Device", tr: "Cihaz", de: "Gerät", fr: "Dispositif", es: "Dispositivo", it: "Dispositivo", ar: "الجهاز", ja: "デバイス" },
  flowDeviceDesc: { en: "Sensing, enclosure, battery and RF behavior.", tr: "Ölçüm, muhafaza, pil ve RF davranışı.", de: "Messung, Gehäuse, Batterie und RF-Verhalten.", fr: "Mesure, boîtier, batterie et comportement RF.", es: "Medición, carcasa, batería y comportamiento RF.", it: "Misura, involucro, batteria e comportamento RF.", ar: "الاستشعار والغلاف والبطارية وسلوك RF.", ja: "センシング、筐体、バッテリー、RF特性。" },
  flowNetwork: { en: "Network", tr: "Ağ", de: "Netzwerk", fr: "Réseau", es: "Red", it: "Rete", ar: "الشبكة", ja: "ネットワーク" },
  flowNetworkDesc: { en: "LoRaWAN payloads and gateway planning.", tr: "LoRaWAN payload'ları ve gateway planlaması.", de: "LoRaWAN-Payloads und Gateway-Planung.", fr: "Payloads LoRaWAN et planification des passerelles.", es: "Payloads LoRaWAN y planificación de gateways.", it: "Payload LoRaWAN e pianificazione dei gateway.", ar: "حمولات LoRaWAN وتخطيط البوابات.", ja: "LoRaWAN payloadとgateway計画。" },
  flowData: { en: "Data", tr: "Veri", de: "Daten", fr: "Données", es: "Datos", it: "Dati", ar: "البيانات", ja: "データ" },
  flowDataDesc: { en: "API ingestion and PostgreSQL schema.", tr: "API veri alımı ve PostgreSQL şeması.", de: "API-Ingestion und PostgreSQL-Schema.", fr: "Ingestion API et schéma PostgreSQL.", es: "Ingesta API y esquema PostgreSQL.", it: "Ingestion API e schema PostgreSQL.", ar: "استقبال API ومخطط PostgreSQL.", ja: "API取り込みとPostgreSQLスキーマ。" },
  flowInterface: { en: "Interface", tr: "Arayüz", de: "Schnittstelle", fr: "Interface", es: "Interfaz", it: "Interfaccia", ar: "الواجهة", ja: "インターフェース" },
  flowInterfaceDesc: { en: "Dashboard, mobile app and admin panel.", tr: "Dashboard, mobil uygulama ve yönetim paneli.", de: "Dashboard, mobile App und Admin-Panel.", fr: "Dashboard, application mobile et panneau admin.", es: "Dashboard, app móvil y panel admin.", it: "Dashboard, app mobile e pannello admin.", ar: "لوحة تحكم وتطبيق جوال ولوحة إدارة.", ja: "ダッシュボード、モバイルアプリ、管理画面。" },
  flowAction: { en: "Action", tr: "Aksiyon", de: "Aktion", fr: "Action", es: "Acción", it: "Azione", ar: "الإجراء", ja: "アクション" },
  flowActionDesc: { en: "Alerts, reports, integrations and service workflows.", tr: "Uyarılar, raporlar, entegrasyonlar ve servis iş akışları.", de: "Alarme, Berichte, Integrationen und Service-Workflows.", fr: "Alertes, rapports, intégrations et workflows de service.", es: "Alertas, informes, integraciones y flujos de servicio.", it: "Avvisi, report, integrazioni e workflow di servizio.", ar: "تنبيهات وتقارير وتكاملات وسير عمل للخدمة.", ja: "アラート、レポート、連携、運用ワークフロー。" }
};

const getT = (key: keyof typeof t) => {
  return t[key][locale] || t[key]["en"];
};
---

<PageLayout
  title={seo.seoTitle || `${product.title} | WillowSoft`}
  description={seo.metaDescription || product.shortDescription || ""}
  canonical={seo.canonical || `${SITE_ORIGIN}/${locale}/products/${slug}`}
  ogTitle={seo.ogTitle || product.title}
  ogDescription={seo.ogDescription || seo.metaDescription || product.shortDescription || ""}
  ogImage={product.image ? `${SITE_ORIGIN}/${product.image}`.replace(/([^:])\/\//g, "$1/") : DEFAULT_OG_IMAGE}
  alternates={buildAlternates(`/products/${slug}`)}
  schema={graphSchema}
  locale={locale}
  dataPage="product-detail"
>
  <main>

    <section class="hero compact">
      <div class="hero-media">
        <img src={product.image ? `/${product.image}`.replace("//", "/") : "/assets/hero-industrial-iot.png"} alt={product.title} loading="lazy" decoding="async" />
      </div>
      <canvas class="signal-canvas" data-density="36"></canvas>
      <div class="hero-inner">
        <p class="eyebrow reveal">{product.category || "Product"}</p>
        <h1 class="reveal delay-1">MODIFIED {product.title}</h1>
        <p class="reveal delay-2">{product.shortDescription}</p>
        <div class="hero-ctas reveal delay-3">
          <a class="btn btn-primary" href={`/${locale}/contact?product=${encodeURIComponent(slug)}`}>{getT("reqInfo")}</a>
          <a class="btn btn-secondary" href={`/${locale}/products`}>{getT("backToProducts")}</a>
        </div>
      </div>
    </section>

    <section class="section tight">
      <div class="section-inner">
        <AIAnswerBlock answer={productAiAnswer} locale={locale} meta={productAiMeta} />
      </div>
    </section>

    <!-- Bento Snapshot -->
    <section class="section">
      <div class="section-inner bento">
        <article class="card accent reveal">
          <p class="eyebrow">{getT("technicalSnapshot")}</p>
          <h2 class="page-title">{snapshotText}</h2>
          <div class="mini-spec-grid">
            {(product.chips || []).slice(0, 3).map((chip: string) => (
              <div class="mini-spec">
                <strong>{chip}</strong>
                <span>{getT("capability")}</span>
              </div>
            ))}
          </div>
        </article>
        <article
          class="card visual-card reveal delay-1"
          style={product.image ? `background-image: url('/${product.image}')` : ""}
        >
          <h3>{getT("readyText")}</h3>
          <p>{getT("readyDesc")}</p>
        </article>
      </div>
    </section>

    <!-- Gallery Section -->
    {galleryImages.length > 1 && (
      <section class="section product-gallery-section">
        <div class="section-inner">
          <div class="section-head">
            <p class="eyebrow">{getT("gallery")}</p>
            <h2 class="page-title">{getT("gallery")}</h2>
          </div>
          <ProductGallery images={galleryImages} title={product.title} />
        </div>
      </section>
    )}

    <!-- Tech sections -->
    {(techBlocks.length > 0 || dynamicFacts.length > 0 || productApplications.length > 0 || dynamicSpecGroups.length > 0) && (
      <section class="section soft product-tech-section">
        <div class="section-inner">
          {techBlocks.map((block) => (
            <article class="product-tech-block" style={{ marginBottom: "40px" }}>
              <h2 class="page-title">{block.label}</h2>
              <div class="rich-content" set:html={block.html} />
            </article>
          ))}
          {dynamicFacts.length > 0 && (
            <article class="product-dynamic-card product-dynamic-card-highlight">
              <p class="eyebrow">{dynamicLabel("productModel")}</p>
              <div class="product-dynamic-facts">
                {dynamicFacts.map((fact) => (
                  <div>
                    <span>{fact.label}</span>
                    <strong>{fact.value}</strong>
                  </div>
                ))}
              </div>
            </article>
          )}
          {productApplications.length > 0 && (
            <article class="product-dynamic-card">
              <p class="eyebrow">{dynamicLabel("applications")}</p>
              <h2 class="page-title">{dynamicLabel("whereUsed")}</h2>
              <ul class="product-dynamic-list">
                {productApplications.map((item: string) => (
                  <li>{item}</li>
                ))}
              </ul>
            </article>
          )}
          {dynamicSpecGroups.map((group: any) => (
            <article class="product-dynamic-card">
              <p class="eyebrow">{dynamicLabel("technicalGroup")}</p>
              <h2 class="page-title">{group.title}</h2>
              {group.rows.length > 0 && (
                <dl class="product-dynamic-specs">
                  {group.rows.map((row: any) => (
                    <div>
                      <dt>{row.label || "Field"}</dt>
                      <dd>{row.value || ""}</dd>
                    </div>
                  ))}
                </dl>
              )}
              {group.lists.map((list: any) => (
                <div class="product-dynamic-sublist">
                  <h3>{list.title}</h3>
                  <ul>
                    {list.items.map((item: string) => (
                      <li>{item}</li>
                    ))}
                  </ul>
                </div>
              ))}
            </article>
          ))}
        </div>
      </section>
    )}

    <!-- Where it fits -->
    <section class="section soft">
      <div class="section-inner split">
        <div class="reveal">
          <p class="eyebrow">{getT("whereFits")}</p>
          <h2 class="page-title">{getT("fitsTitle")}</h2>
        </div>
        <div class="reveal delay-1">
          <p class="section-lead">{fitText}</p>
          <ul class="spec-list">
            <li>{getT("fitsLi1")}</li>
            <li>{getT("fitsLi2")}</li>
            <li>{getT("fitsLi3")}</li>
          </ul>
        </div>
      </div>
    </section>

    <!-- Buyer Checklist -->
    <section class="section product-decision-section">
      <div class="section-inner">
        <div class="section-head reveal product-headline">
          <div>
            <p class="eyebrow">{getT("buyerChecklist")}</p>
            <h2>{getT("buyerTitle")}</h2>
          </div>
          <a class="btn btn-secondary btn-small" href={`/${locale}/start-project`}>{getT("planDeployment")}</a>
        </div>
        <div class="product-decision-grid">
          <article class="product-decision-card reveal">
            <span>{getT("useCase")}</span>
            <h3>{decision.useTitle}</h3>
            <p>{decision.use}</p>
          </article>
          <article class="product-decision-card reveal delay-1">
            <span>{getT("systemRole")}</span>
            <h3>{decision.roleTitle}</h3>
            <p>{decision.role}</p>
          </article>
          <article class="product-decision-card reveal delay-2">
            <span>{getT("deliveredWith")}</span>
            <h3>{getT("hwSwPath")}</h3>
            <ul>
              {decision.delivered.map((item: string) => (
                <li>{item}</li>
              ))}
            </ul>
          </article>
        </div>
      </div>
    </section>

    <!-- System Architecture -->
    <section class="section soft product-system-section">
      <div class="section-inner split">
        <div class="reveal">
          <p class="eyebrow">{getT("sysArch")}</p>
          <h2 class="page-title">{getT("sysTitle")}</h2>
          <p class="section-lead">{getT("sysDesc")}</p>
        </div>
        <div class="tech-flow product-tech-flow reveal delay-1">
          <div class="flow-row">
            <div class="flow-node"><small>01</small><strong>{getT("flowDevice")}</strong><span>{getT("flowDeviceDesc")}</span></div>
            <div class="flow-node"><small>02</small><strong>{getT("flowNetwork")}</strong><span>{getT("flowNetworkDesc")}</span></div>
            <div class="flow-node"><small>03</small><strong>{getT("flowData")}</strong><span>{getT("flowDataDesc")}</span></div>
            <div class="flow-node"><small>04</small><strong>{getT("flowInterface")}</strong><span>{getT("flowInterfaceDesc")}</span></div>
            <div class="flow-node"><small>05</small><strong>{getT("flowAction")}</strong><span>{getT("flowActionDesc")}</span></div>
          </div>
        </div>
      </div>
    </section>

    <!-- Quote band -->
    <section class="section soft services-faq-section" id="faq">
      <div class="section-inner">
        <div class="section-head reveal center" style={{ marginBottom: "36px" }}>
          <p class="eyebrow">FAQ</p>
          <h2 class="page-title">{product.title} FAQ</h2>
          <p class="section-lead" style={{ maxWidth: "720px", marginLeft: "auto", marginRight: "auto" }}>
            {stripHtml(fitText)}
          </p>
        </div>
        <div class="faq-list reveal delay-1">
          {productFaqItems.map((faq) => (
            <details class="faq-item">
              <summary>
                <span class="faq-q">{faq.question}</span>
                <span class="faq-toggle" aria-hidden="true"></span>
              </summary>
              <p>{faq.answer}</p>
            </details>
          ))}
        </div>
      </div>
    </section>

    <section class="section product-quote-band">
      <div class="section-inner product-quote-inner reveal">
        <div>
          <p class="eyebrow">{getT("needInProject")}</p>
          <h2 class="page-title">{getT("askProductFit")}</h2>
        </div>
        <div class="product-quote-actions">
          <a class="btn btn-primary" href={`/${locale}/start-project?product=${encodeURIComponent(slug)}`}>{getT("startProject")}</a>
          <a class="btn btn-secondary" href={`/${locale}/contact?product=${encodeURIComponent(slug)}`}>{getT("contactSales")}</a>
        </div>
      </div>
    </section>
  </main>
</PageLayout>
